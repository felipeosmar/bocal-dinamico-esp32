meta {
  name: Remove actuator
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/actuator/remove
  body: json
  auth: inherit
}

example {
  name: 200 Response
  description: Remove result
  
  request: {
    url: {{baseUrl}}/api/actuator/remove
    method: POST
    mode: json
    body:json: {
      {
        "id": 3
      }
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
